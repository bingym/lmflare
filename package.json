{
  "name": "lmflare",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "mkdir -p web/dist",
    "postinstall": "npm ci --prefix web",
    "dev": "wrangler dev",
    "dev:web": "npm run dev --prefix web",
    "build:web": "npm ci --prefix web && npm run build --prefix web",
    "deploy": "npm ci --prefix web && npm run build --prefix web && wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply lmflare-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply lmflare-db --remote"
  },
  "dependencies": {
    "hono": "^4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "typescript": "^5",
    "wrangler": "^4"
  }
}
